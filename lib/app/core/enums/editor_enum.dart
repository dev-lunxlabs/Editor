



enum BtnSet {
  video,
  audio,
  text,
  elements,
  filter,
}

enum VideoBtnSet {
  trim,
  crop,
  concatenate,
  split,
  cover,
  speed,
  flip,
  aspectRatio,
  transition,
}

enum AudioBtnSet {
  add,
  remove,
  extract,
}

enum TextBtnSet {
  text,
  autoCaption,
}

enum ElementBtnSet {
  brush,
  stickers,
  image,
  video,
  border,
  watermark,
  background,
}

enum FilterBtnSet {
  template,
  color,
  contrast,
}


