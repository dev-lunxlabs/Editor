import 'package:flutter/material.dart';

class CropTransform extends StatelessWidget {
  const CropTransform({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ClipRRect();
  }
}
